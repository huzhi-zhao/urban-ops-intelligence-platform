# 评分链与 M1（L3）

> **Status**: Draft（框架） · **Date**: 2026-08-17
>
> **上游需求**: [20260817-etl-implementation.md](20260817-etl-implementation.md)（E5 + E6）
> **前置上线**: [L1 Silver 全链路跑通](20260817-silver-etl-runnable.md) →
> [L2 Gold 维表与事实表](20260817-gold-dimensional-build.md) —— **两者都是硬前置**
> **相关**: [ADR 0010](../adr/0010-gold-fact-grain-and-dimension-layering.md) ·
> [metric-feasibility-audit.md](../requirements/metric-feasibility-audit.md)（三项独立性的实测依据）
>
> ⚠️ **本篇是框架,不是可执行计划。** L3 依赖 L2 交付的 13,068 行训练面板,
> 在那之前写的任何训练细节都是纸上的。细化是 L2 上线之后的专门任务。

---

## 1. 问题

L2 交付之后,Gold 还剩 **3 张表零行**,而它们承载的正是项目对外要讲的东西:

| 表 | 是什么 |
|---|---|
| `fact_request_forecast`（F5） | M1 的预测输出 |
| `fact_winter_event_zone_load`（F6） | 冬季运营负荷评分（BO-6 三因子公式） |
| `fact_recommendation`（F7） | 资源调配建议 |
| —— | 加上 E6 的 DQ 基线与 S7 schema 冻结 |

**为什么它不是 ETL。** F5 要训练模型,F6/F7 是打分和生成建议文本。
虽然物理上都是 Gold 表、都用 `INSERT OVERWRITE PARTITION` 写,
但失败模式完全不同:ETL 的失败是「数字不对」,建模的失败是「数字对但结论站不住」。
两者放进一次上线,验收判据没法写在同一个尺度上。

另一件绕不开的:advisor 定死的 "AI-Driven" 标题让 ML 层成为必须项,
所以 L3 不是「有余力再做」的一批。

## 2. 已定死、L3 不重开的口径

这些是探针实测出来的结论,**不是设计假设**,L3 只能照做:

- **三因子独立性成立,公式不改残差形式、权重不重分配。** 三项两两 |r| 最大仅 +0.460;
  `r(顺位, 请求量) = +0.017`、`r(顺位, 天气) = −0.006`。删掉顺位项在 15/15 个事件上
  都改变分区排序。
- 🔴 **天气项不得表述为「影响调度建议」。** 它的方差 **99.4% 在事件之间**、
  仅 0.6% 在事件之内 —— 决定评分高低,几乎不影响事件内排序,而 BO-8 消费的正是排序。
- 🔴 **实际影响序与名义权重相反**:顺位(0.300) > 请求量(0.270) > 天气(0.167) 分数单位,
  名义权重是 0.40/0.30/0.30。对外表述按实际影响序。
- **请求量因子的地址数分母是承重的**:去掉它 `r(顺位, 请求量)` 从 +0.017 虚涨到 +0.139。
  这条也是 `silver_snow_clearing_address` 存在的唯一理由。
- `weather_severity_factor` 在 H1 内是**事件级常量**（A2 方案②）,
  断言「同事件跨分区取值相同」。
- **`dim_recommendation_rules` 是文字模板 + 降级兜底,不得称之为 AI。**
- 满面板 **1,298** 格（22 × 59 排班期事件）,缺失用 `score_status` 表达
  （预期 `scored` ≈ 374、`partial_no_rank` ≈ 924）,
  `score_weight_profile` ∈ {`full_3factor`, `demand_weather_only`} 与 `score_status` 绑死。
- `fact_recommendation` 的 PK 含 `model_version`;带 `rank_model` / `rank_baseline` / `rank_delta`。

## 3. 框架:三段

```
L3-a M1 训练与 F5  ──> L3-b 评分链 F6/F7  ──> L3-c DQ 基线 + S7 冻结
(Python)              (sql/intelligence/)      (E6)
```

- **L3-a**:M1 读 F1（**含**类别维度）训练,预测时**不含**类别维度,
  `predicted_count` = 跨类别求和;必须带 `baseline_count` 与 `model_version` ——
  没有 baseline 的模型分数无法说明它比什么强。
- **L3-b**:`sql/intelligence/`,满面板 1,298 行,不是只存有分的格子。
- **L3-c**:每张表记录行数 · 各列空值率 · 分区完整性 · 构建耗时。
  **这是后续告警阈值的唯一依据**,没有基线的阈值都是拍脑袋。
  基线数字在 L1/L2 跑完的当时就分别记进各自的 launch,L3 只做汇总 + 补测试四件套
  （`unique` / `not_null` / `relationships` / `accepted_values`）。

### 已知的硬判据

| 判据 | 值 |
|---|---|
| F6/F7 满面板 | **1,298** 格 |
| 非零率 | **70.57%（916 格）** |
| `rank_factor = 0` 的行数 | **0**（顺位缺失表示为 NULL,不是 0） |
| `score_status` 分布 | `scored` ≈ 374 / `partial_no_rank` ≈ 924。偏离说明犁雪↔降雪对齐逻辑变了,回查 A1/B1 |

## 4. 开放项

| # | 未定的事 | 现有建议 | 时点 |
|---|---|---|---|
| **O1** | M1 的模型形态、特征集、评估口径**完全未定** | L2 交出 13,068 行面板之后再定 —— 面板的稀疏结构（零请求是显式信号）会直接决定能用什么模型 | L3 细化第一件事 |
| **O2** | 顺位窗口:BO-6 的 0.30 权重**不得喂十年均值**（前后半期 ρ = +0.591） | 与 L2 O2 用**同一个近期窗口**,两处不能各取一套 | 继承 L2 的决定 |
| **O3** | BO-3 事件定义若因滚动累积判据改变 N,回测次数与面板格数同步改 | **不改 schema**,只改行数判据 | L3 开工前确认 N 已冻结 |
| **O4** | 对外表述的边界:哪些能说「AI 驱动」,哪些必须说「规则模板」 | F5 是模型,F6 是加权公式,F7 是文字模板。三者对外不能混为一谈 | 讲稿之前 |

## 5. 时间盒（占位）

伞篇 §7 给的是 8/31–9/6（E5）+ 9/14–9/19（E6 + 讲稿）,9/7–9/13 是缓冲。
**缓冲不吃进 L3 的范围** —— 出问题在缓冲里解决,不是拿缓冲加功能。
会期 2026-09-19 不挪,contract 冻结线不挪。
