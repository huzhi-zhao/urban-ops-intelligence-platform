# 政策文件考古探针 上线记录

> **Date**: 2026-08-31 · **Design**: [../design/20260831-policy-document-research-probe.md](../design/20260831-policy-document-research-probe.md)
> **Result**: 🔴 **预检不通,M 批未执行** —— 目标域名被本环境出站策略拒绝（2026-09-01）

## 0. 为什么提前开篇

与 L3 那篇同一条理由，而与其余七篇都不同：**不是变更不可逆，是结论只在采集的那一刻存在。**

市政网站改版、撤稿、换链接是常态。一条今天能打开的 URL，三个月后可能 404，
而那时无法自证当初读到的是什么。因此：

- **快照先于引文**。§3 台账里没有 `快照` 一列的行，等于没查过。
- **检索日与 URL 同等重要**，两者缺一不可。
- 台账**在查的当时逐条填**，不在事后回忆着补。

本次**没有任何一步不可逆**，也不触碰生产数据：只读公开文档，不打 SODA、
不碰 MinIO / Trino / Silver。唯一会改动仓库既有文档的动作是 design G4，
且只在 Q3 命中时发生。

---

## 1. 执行结构：H0 → M → H1

分工判据与批次设计见 [design §3.4](../design/20260831-policy-document-research-probe.md)。
**人只出现在头尾，中间三小时无人值守。**

### 1.1 H0 · 人 · 5 分钟

- [ ] 批准 design（本篇 §4 的门禁与 §3 的字段要求随之冻结）

出口：design 状态为 Accepted。**H0 结束即可离开**，M 批不依赖任何外部答复。

### 1.2 M · 机器 · ≤ 3 小时（无人值守）

| 时刻 | 段 | 动作 | 结果 |
|---|---|---|---|
| 2026-09-01 | 预检 | 试取目标域名 | 🔴 **不通,当场终止** |
| — | A / B / C | —— | **未执行** |

### 预检实测（2026-09-01）

| 试法 | 结果 |
|---|---|
| `WebFetch` `www.winnipeg.ca` | `EGRESS_BLOCKED` |
| `WebFetch` `legacy.winnipeg.ca` | `EGRESS_BLOCKED` |
| `curl` `legacy.winnipeg.ca` | `CONNECT tunnel failed, response 403` |
| `curl` `winnipeg.ca` · `clkapps.winnipeg.ca` · `web.archive.org` | 全部 `000` |
| **Chromium**（headless，同一代理） | `ERR_TUNNEL_CONNECTION_FAILED` |
| `WebSearch` | ✅ 通 |

代理自陈：`connect_rejected · gateway answered 403 to CONNECT (policy denial)
· legacy.winnipeg.ca:443`。

🔴 **这不是登录墙、验证码或注册墙——是本环境的出站策略拒绝该域名。**
因此**换浏览器不解决**：Chromium 走同一条隧道，撞同一堵墙，
错误页里没有任何可点的东西。这一条值得单独记下来，因为
「抓不到就开浏览器手动点」是一条看起来总该有效、而在本环境下**先验无效**的退路——
判据是错误发生在 `CONNECT` 阶段，早于任何页面内容存在。

🟡 **`WebSearch` 通,但它救不了本探针。** 它返回的是检索服务对页面的**摘要**，
不是页面原文，满足不了 §3.3 的「逐字引文 + 页码 + 快照」。
把它当命中记账，等于用一条无法复核的转述去修一句无出处的转述——
正是 design §1 要解决的那个毛病。它只能作**线索**，见 §3。

🔴 **中途不提问。** 遇到需要人判断的分叉，两条都记进 §3 并标
`⚠️ 待 H1 裁决`，继续往下跑。

🔴 **唯一允许中途停下的情况是预检不通**（出站被代理拦、市政站点取不到）。
那时**立刻退回 H0**，不要在不确定网络能力的前提下跑满三小时再报告失败。

### 1.3 H1 · 人 · 20 分钟（一次坐下，把判断一起做完）

- [ ] 逐条看 §3 里标 `⚠️ 待 H1 裁决` 的边缘一手性判定
- [ ] Q3 命中？→ 决定是否执行 G4（改 `business-objectives.md` §987）
- [ ] Q1/Q2 采纳 / 放弃
- [ ] design §6 的开放项各给一个状态

### 1.4 回滚点

**全程可退**，没有一步产生不可逆后果：只读公开文档，不打 SODA、
不碰 MinIO / Trino / Silver。唯一改动仓库既有文档的动作是 G4，
它在 H1 由人执行、单独一次改动。

---

## 2. 与设计的偏差

| 设计怎么写的 | 实际怎么做的 | 为什么改 |
|---|---|---|
| 待填 | | |

> 说「没有偏差」也要写一行——那是对设计质量的正面证据。

---

## 3. 证据台账

**一条证据一块，六个字段缺一不可**（design §3.3）。逐字引文，不转述、不翻译。

### Q3 · P1–P3 街道等级的官方定义

```
状态:
引文:
出处:
检索日:
一手性:
快照:
```

### Q1 · 分级制度的建立年份

```
状态:
引文:
出处:
检索日:
一手性:
快照:
```

### Q2 · 当年写下的理由原话

```
状态:
引文:
出处:
检索日:
一手性:
快照:
```

### 🟡 线索（**不是证据**，不得引用）

`WebSearch` 在预检时顺带返回了指向目标文件的 URL，以及检索服务对页面内容的摘要。
**两者的地位完全不同**：

| | 地位 |
|---|---|
| **URL 本身** | ✅ 可靠且有用——它把「去哪找」这一问解决了，M 批恢复后段 A 可直接跳过 |
| **摘要内容** | 🔴 **不得进入 §3 台账**。它是检索服务对页面的转述，不是原文，无页码、无快照 |

已定位到的目标（**未读取,未快照**）：

| # | URL | 预期回答 |
|---|---|---|
| 1 | `legacy.winnipeg.ca/publicworks/snow/snow-clearing-policy.stm` | Q3 主目标；可能含 Q1/Q2 |
| 2 | `legacy.winnipeg.ca/publicworks/snow/street-priority.stm` | Q3 的分级定义 |
| 3 | `winnipeg.ca/services-programs/transportation-roads/maintenance/snow-clearing-ice-control/frequently-asked` | Q3 二手措辞（§3.2 第 3 类） |
| 4 | `clkapps.winnipeg.ca`（市书记官文件库） | Q1/Q2 的委员会报告与会议记录 |

🔴 摘要里出现了若干**具体数字**（清雪时限小时数、触发厚度、分级依据）。
它们与仓库既有记载方向一致，**但正因为一致才更危险**——
一个未经原文核对的数字，一旦因为「看着对」被写进台账，就再也没有人会去核它。
本篇**不记录这些数字**，一个都不记。

---

### 检索轨迹（未命中时这一节就是产出）

按 design §3.2 的五类源逐类记：查了什么、怎么查的、为什么没有。
🔴 **未命中是合法交付**，但「没查到」必须能被复核——空着的一节不算交付。

| 源类 | 查了什么 | 结果 |
|---|---|---|
| 1 一手政策 | | |
| 2 委员会报告 / 会议记录 | | |
| 3 公众页 | | |
| 4 综述 | | |
| 5 新闻（仅定位用） | | |

### ⚠️ 待 H1 裁决

M 批遇到需要人判断的分叉时，**两条都记在这里并继续跑**，不中途提问。
H1 一次性裁完。空着表示 M 批没遇到分叉。

| # | 分叉是什么 | 选项 A | 选项 B | H1 裁决 |
|---|---|---|---|---|
| 1 | 目标域名被出站策略拒绝，M 批无法自行取文 | **放开 `winnipeg.ca` 出站**（环境网络策略，见 [Claude Code on the web 文档](https://code.claude.com/docs/en/claude-code-on-the-web)），M 批原样重跑、仍是 3 小时无人值守 | **人工取文**：把上表四个 URL 的页面/PDF 存进 `var/research/snow-policy/`，M 批从**段 B** 接手（摘引 + 台账 + 裁决草案，约 90 分钟） | 待裁决 |

**建议 A。** 它保住了原设计的形态——一次批准之后三小时不用管；
B 要人先干 20–30 分钟的取文活，正是本篇力图避免的那类切换。
🔴 **但两条都不要选「就用 `WebSearch` 的摘要」**：那会让 §3.3 的六字段
形同虚设，而那六个字段是本探针**唯一**区别于「想起来查一句」的东西。

---

## 4. 验收判据的实际结果

逐条对照 design §5，**贴真实内容，不写「已验证」**。

| # | 判据 | 结果 |
|---|---|---|
| G1 | Q1/Q2/Q3 各有一行裁决，无空缺 | ⏸ **未达**——三问均未进入检索，状态为「未执行」而**不是「未命中」**。两者不可混：未命中是查过没有，未执行是没查 |
| G2 | 每条「命中」六字段齐全，引文非空、快照存在 | ⏸ 空适用（无命中） |
| G3 | 无一条结论的一手性为「新闻」 | ✅ **达成**——本篇未记录任何结论 |
| G4 | Q3 命中 → `business-objectives.md` §987 补上出处 | ⏸ 未触发。§987 维持原状 |
| G5 | 全程 ≤ 3 小时 | ✅ **达成**——预检在数分钟内终止，未消耗时间盒 |

🟢 **G5 达成的方式恰恰是设计想要的**：预检存在的全部理由，就是让这种失败
在第一分钟暴露，而不是三小时后。若无这道预检，本轮会在反复重试与绕路上
耗掉整个时间盒，最后交出同一份「没查到」。

---

## 5. 遗留项

| 遗留 | 去处 |
|---|---|
| Q1 / Q2 / Q3 全部**未执行** | 等 §3「待 H1 裁决」第 1 条选定后，M 批继续 |
| 四个已定位 URL 未读取、未快照 | 同上。**URL 本身已是产出**——恢复后段 A 可跳过 |
| design §6 的 P1–P6 | 均未触及，维持原状 |

design §6 的开放项（P1 口径实质冲突 / P2 SLA 时限 / P3 已关闭 /
P4 信息公开申请 / P5 迟到的一手文件 / P6 与 portfolio §4.4 的冲突）
在段 C 结束时逐条给状态。

### 恢复入口（换会话后从这里读起）

前提：环境网络策略已放开 `winnipeg.ca`（§3 待 H1 裁决第 1 条选 A）。

1. **预检**：`WebFetch` `legacy.winnipeg.ca/publicworks/snow/snow-clearing-policy.stm`。
   这一步同时是**策略是否真的生效**的验证——仍被拒说明改动没落到这个环境上，
   不要绕路，回头排查配置。
2. **段 A 跳过**：四个目标 URL 已定位，见 §3「线索」表。
3. 从**段 B** 开始：精读 → 逐字摘引 → 存快照到 `var/research/snow-policy/` →
   填 §3 三块证据卡的六个字段。
4. 段 C：写裁决草案，自评 §4 的 G1–G3、G5，交回 H1。

🔴 恢复后仍**不记录**任何来自 `WebSearch` 摘要的数字（理由见 §3「线索」）。
🔴 恢复后仍**不中途提问**：分叉记进 §3「待 H1 裁决」，继续跑。

---

## 6. 上线后需要观察的

只有一条，而且它不随时间衰减：

🔴 **凡是引用了本次结论的地方，必须同时携带出处与检索日。**
一条政策引文脱离出处之后，与一句无出处的转述在文本上无法区分——
而 design §1 的整个问题，就是从一句无出处的转述开始的。
