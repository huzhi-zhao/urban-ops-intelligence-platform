# Bronze Socrata 分页无序 故障复盘

> **Date**: 2026-08-18 · **Detected**: 2026-08-18 14:42（L1 阶段 E 全量回填第 10 个窗口）
> **Impact**: `SRC-WPG-311` 的 Bronze 存量中存在分页边界造成的重复行，
> 且**同一机制会造成无法被任何现有检查发现的丢行**。影响面待 §1 的扫描填数。

## 1. 影响

**已确认**：`bronze/raw/SRC-WPG-311/service_requests/2019-06/data_2019-06-07.ndjson.gz`
（3,585 行，跨 4 页）中，`case_id=103d95de… / interaction_id=101012333164`
一行**字节级重复两次**。该文件内只此一个重复键。上游实测该键只有一条记录
（同 `interaction_id` 的另一条 `case_id` 不同，属正常的一通电话派生两个 case），
故重复是**采集侧制造的**，不是上游数据形态。

**待定（扫描中）**：全量 Bronze 里还有多少分片带重复。
入口 `python -m scripts.profiling.bronze_duplicate_scan --bucket uoip
--source SRC-WPG-311 --dataset service_requests --key case_id --key interaction_id`。

> ⚠️ **不可用扫描结果证明「没问题」**。同一根因的另一半是**丢行**：
> 页边界移位既能让一行出现两次，也能让一行一次都不出现。丢掉的行在分片里
> 不留任何痕迹——没有计数对不上、没有校验和异常、manifest 里的
> `sha256_checksum` 描述的正是那份已经少了行的载荷。所以**一个"干净"的
> 多页分片不构成任何证据**，唯一的确认手段是按修好的 fetcher 重拉后对行数。

**未受影响**：Silver / Gold 一行都没写进去——`assert_unique` 在写盘前抛出。
L1 阶段 E 前 9 个窗口（雪季窗口）跑完的分区数据本身有效，但它们同样来自
未修复的 fetcher，可信度与上面同一条：不能因为断言没响就认为它们完整。

## 2. 时间线

| 时点 | 事件 |
|---|---|
| 2026-06-15 | `ingestion/backfill/fetchers/socrata.py` 建立，`_fetch_window` 无 `$order` |
| 2026-08-02 | `5b7b14a` 给 `_fetch_full_table` 加 `$order=:id`，并写下"unordered limit/offset paging 会重复或丢行"的注释。**同一份文件里的窗口分支未同步** |
| 2026-08-02 起 | 311 全量 Bronze 回填按 8 个雪季窗口 + 全量段执行完毕，带缺陷 |
| 2026-08-18 14:42 | L1 阶段 E 第 10 个窗口（2019 全年）`assert_unique` 抛出：930,614 行 / 930,613 distinct |
| 2026-08-18 15:35 | 定位到重复行，确认字节级相同、同一分片内 |
| 2026-08-18 15:40 | 上游实测确认 `(case_id, interaction_id)` 在上游唯一，判定为采集侧缺陷 |
| 2026-08-18 | `_fetch_window` 补 `$order=:id` + 单测；扫描探针落地 |

**从引入到发现：64 天**（6/15 → 8/18）。**从"带缺陷的数据落盘"到发现：16 天**。

## 3. 根因

**技术根因**：Socrata 的 limit/offset 分页在结果集无序时不保证行序。
`$order=:id` 只加在了全表walk 分支，窗口分支漏了。**过滤不会让结果集变得有序**
——这是当时判断中缺的那一步：写注释的人正确地识别了风险，但把风险归因给了
"全表walk 很长"，而真正的条件是"跨页"。311 单日 3,585 行，天天跨页。

**流程根因**：`5b7b14a` 修的是"static 源一行都取不到"这个眼前故障，
`$order=:id` 是顺带加的正确补丁。修一个具体故障时，**没有反问同一份文件里
另一条代码路径是否有同样的问题**——两个分支相隔 30 行。

## 4. 为什么没有更早发现

- **Bronze 侧没有任何唯一性检查**。`dag_audit_bronze` 核对的是"分区在不在、
  manifest 有没有"，不核对分区**内容**是否自洽。重复行和丢行都不改变分区存在性。
- **manifest 的校验和防不住这个**。`sha256_checksum` 描述的是我们写下去的载荷，
  不是上游应有的载荷；采集时就重了/少了，校验和会忠实地确认那份错误载荷完整无损。
  它保证的是"传输与存储没出错"，从来不保证"取的是对的"。
- **它最终是被下游一层的断言发现的**，而且是被 Silver 的 PK 断言**顺带**发现的
  ——那条断言是为"上游变更"设计的，不是为"采集缺陷"设计的。
  它抓到了，属于运气好的一侧：换成丢行，这条链上没有任何一环会响。
- 探针阶段（任务 3）跑出的 311 空间命中率 134,123/134,258 走的是**直连 API 的
  取数层**（`scripts/analysis/_probe_common.py`），不经这条 fetcher，
  所以探针数字与 Bronze 的分歧不会以任何形式暴露出来。

## 5. 改进项

| 改什么 | 类型 | 落到哪 |
|---|---|---|
| `_fetch_window` 加 `$order=:id`，注释写明"过滤不使结果集有序" | 止血 | 本次 PR，`ingestion/backfill/fetchers/socrata.py` |
| 单测断言窗口式抓取传 `order_by=":id"`（此前只有全表分支有此断言） | 防复发 | 本次 PR，`tests/unit/test_backfill_fetchers.py` |
| Bronze 重复键扫描探针 | 早发现 | 本次 PR，`scripts/profiling/bronze_duplicate_scan.py` |
| 按扫描结果重拉受影响的 Bronze 日分片，再重跑 L1 阶段 E | 止血 | L1 launch 篇 §2 阶段 E |
| **重拉后对行数**——丢行只能这样确认 | 早发现 | 同上，数字进 launch |
| `dag_audit_bronze` 增加分片内 PK 唯一性核对（目前只核对分区存在性） | 早发现 | Ticket（L2 之前，不占 L1 关键路径） |

> 一条不属于改进项但值得记的判断：**`assert_unique` 不改**。
> 它这次抓到的正是它该抓的东西，把它换成 `dropDuplicates` 会把这整篇复盘
> 变成一张悄悄短了一行的表。
